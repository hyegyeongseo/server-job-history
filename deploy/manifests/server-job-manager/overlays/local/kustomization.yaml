# Tier 1 · overlay (local) — 이미지 치환 + (Step 1.6) OTEL 켜기 패치
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
images:
  - name: REPLACED_BY_OVERLAY
    newName: ghcr.io/hyegyeongseo/server-job-manager      # 레지스트리 주소: GHCR/Docker Hub/로컬 레지스트리
    newTag: "0.1.0"                             # ★ docker push 한 태그와 동일
# Tempo 준비 후 OTEL_TRACING_ENABLED=true
#patches:
#  - path: configmap-otel-patch.yaml
#    target: { kind: ConfigMap, name: server-job-manager-config }
